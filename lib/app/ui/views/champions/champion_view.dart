import 'package:flutter/material.dart';

class ChampionView extends StatelessWidget {
  const ChampionView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Champions"),
      ),
    );
  }
}
